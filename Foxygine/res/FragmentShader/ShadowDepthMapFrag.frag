#version 330 core

//layout(location = 0) out vec3 FragColor;
//out vec4 FragColor;

//in vec3 pos;
//in vec2 uv;

void main(){
    // Not really needed, OpenGL does it anyway
    //gl_FragDepth = gl_FragCoord.z;
    //FragColor = vec3(vec3(gl_FragColor.z));
    //FragColor = vec4(vec3(gl_FragCoord.z), 1);
}